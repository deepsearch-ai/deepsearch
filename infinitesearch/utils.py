from sources import utils as source_utils
from datatype import DataType


def infer_type(source: str) -> DataType:
    pass


def add_data(source: str) -> DataType:
    source_utils.infer_type(source)
